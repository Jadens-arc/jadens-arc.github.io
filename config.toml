# The URL the site will be built for
base_url = "https://jadens-arc.github.io/

# The site title and description; used in feeds by default.
title = "Jaden Arceneaux"
description = "Something or another"

theme = "archie-zola"
# Whether to automatically compile all Sass files in the sass directory
compile_sass = true

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = true

taxonomies = [{ name = "tags" }]

default_language = "en"

[markdown]
# Whether to do syntax highlighting
# Theme can be customised by setting the `highlight_theme` variable to a theme supported by Zola
highlight_code = true

highlight_theme = "OneHalfLight"

[extra]

# control dark mode: auto | dark | toggle
mode = "toggle"

# interal CDN resource
useCDN = false

favicon = "/icon/favicon.png"

copyright = "jadens-arc"

ga = "G-6YGDB8SLBT"

[extra.translations]
languages = [{name = "en", url = "/"}]

[[extra.translations.en]]
show_more = "Read more ⟶"
previous_page = "← Previous"
next_page = "Next →"
posted_on = "on "
posted_by = "Published by"
read_time = "minute read"
all_tags = "All tags"
menus = [
    { name = "Home", url = "/", weight = 2 },
    { name = "All posts", url = "/posts", weight = 2 },
    { name = "About", url = "/about", weight = 3 },
    { name = "Tags", url = "/tags", weight = 4 },
]

# config social icon info in the footer
[[extra.social]]
icon = "github"
name = "GitHub"
url = "https://github.com/jadens-arc"

[[extra.social]]
icon = "linkedin"
name = "LinkedIn"
url = "https://www.linkedin.com/in/jaden-arceneux/"

[[extra.social]]
icon = "instagram"
name = "Instagram"
url = "https://www.instagram.com/jaden.svg"
